package gui;

import javax.swing.ImageIcon;
import javax.swing.JFrame;
import javax.swing.JLabel;

@SuppressWarnings("serial")
public class Connecting extends JFrame{

	private ImageIcon icon = new ImageIcon("images/blinking-red-dot.gif");
	private JLabel centertext = new JLabel ("Establishing Connection...", icon, JLabel.CENTER);
	public void first() {
		
		
		getContentPane().add(centertext);
		setTitle("GPS");
		setSize(500,300);
		setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		setVisible(true);
		setLocationRelativeTo(null);
		
	}

}


